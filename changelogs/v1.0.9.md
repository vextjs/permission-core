# v1.0.9

Released: 2026-06-09

## Fixed

- Refreshed `package-lock.json` with npm 11 so Node 24 publish workflow installs optional native packages consistently.
- Keeps the exact dependency manifest and build-before-publish guard from the previous attempts.

## Verification

- `npm ci` with npm 11
- `npm run typecheck`
- `npm run build`
- `npm pack --dry-run --json`
- Registry fresh install/import smoke after publish
