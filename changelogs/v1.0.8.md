# v1.0.8

Released: 2026-06-09

## Fixed

- Updated the tag publish workflow to `actions/checkout@v6`, `actions/setup-node@v6`, and Node.js 24 to avoid runner action-runtime failures.
- Kept the build-before-publish guard from `v1.0.7`, so the registry tarball includes `dist/` output.

## Verification

- `npm run typecheck`
- `npm test`
- `npm run build`
- `npm pack --dry-run --json`
- Registry fresh install/import smoke after publish
