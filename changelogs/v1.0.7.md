# v1.0.7

Released: 2026-06-09

## Fixed

- Added a publish workflow build step so the registry tarball includes `dist/` output.
- Added `prepublishOnly` as a local and workflow guard before npm publish.

## Verification

- `npm run typecheck`
- `npm test`
- `npm run build`
- `npm pack --dry-run --json`
- Registry fresh install/import smoke after publish
