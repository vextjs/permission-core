# v1.0.6

Released: 2026-06-09

## Changed

- Pinned direct runtime dependencies in `package.json` to exact versions already resolved by `package-lock.json`.
- Pinned direct development dependencies for deterministic local, CI, and publish installs.

## Verification

- `npm run typecheck`
- `npm test`
- `npm run build`
- `npm pack --dry-run --json`
